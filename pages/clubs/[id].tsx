import React from 'react';
import ClubDetail from "@/components/clubs/ClubDetail";

const Detail= () => {
    return (
        <div>
            <ClubDetail/>
        </div>
    );
};

export default Detail;